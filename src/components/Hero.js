import React from 'react';

import Form from '../components/Form';

const Hero = () => {
  return (
    <div className='hero'>
      <Form />
    </div>
  );
};

export default Hero;

/* style={{
        backgroundImage: `url(${background})`,
      }}*/
